[pytest]
testpaths = tests
markers =
    llm: requires a live OpenAI key and costs money (deselect with -m "not llm")
    index: requires a built Chroma index (run scripts/ingest.py first)
filterwarnings =
    ignore::UserWarning
addopts = -q --tb=short
